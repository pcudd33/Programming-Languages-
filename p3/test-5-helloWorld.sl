//hello world

class Main : IO {

    //Main.main() gets run when we execute
    main(){
        print_string("Hello, world.\n");
    };

/*there is not a closing } where is should be and this comment also has no end so EOF